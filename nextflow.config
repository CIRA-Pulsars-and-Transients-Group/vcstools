process {
    withLabel: 'gpu|cpu' {
        executor = 'slurm'
    }
    withLabel: gpu {
        queue = 'gpuq'
        cpus = 3
        memory = "8 GB"
    }
    withLabel: cpu {
        queue = 'gpuq'
        cpus = 1
        memory = "1 GB"
    }
}