process {
    withLabel: 'gpu|cpu' {
        executor = 'slurm'
    }
    withLabel: gpu {
        queue = 'gpuq'
        cpus = 1
        memory = "8 GB"
    }
    withLabel: cpu {
        queue = 'gpuq'
        cpus = 1
        memory = "2 GB"
    }
}
